CAPI=2:
name: fusesoc:pcie:lint:1.0.0
description: pcie linter common

filesets:
  linter:
    files:
        - waiver.vlt : {file_type : user, copyto : .} 

targets:
  # The "default" target is special in FuseSoC and used in dependencies.
  # The "&default" is a YAML anchor referenced later.
  default: &default
    filesets:
      - linter
    toplevel: waiver
